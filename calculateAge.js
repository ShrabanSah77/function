// write a js function to calculate the age of
// the person from date of birth.

const birthYear = 2000;
function calcAge(year) {
  return 2024 - year;
}
const age = calcAge(birthYear);
console.log(age);
